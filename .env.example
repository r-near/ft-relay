# =======================
#  FT Relay Environment
# =======================
# Copy this file to `.env` and adjust the values for your deployment.

# Account that owns and distributes the fungible tokens.
ACCOUNT_ID=distributor.testnet

# Comma-separated list of ed25519 secret keys for ACCOUNT_ID.
# More keys allow the relay to sign multiple transactions in parallel.
PRIVATE_KEYS=ed25519:example_key_one,ed25519:example_key_two

# RPC endpoint the relay should talk to (sandbox, testnet, or mainnet).
RPC_URL=https://rpc.testnet.near.org

# Optional: bind address for the HTTP server (defaults to 0.0.0.0:8080).
# BIND_ADDR=0.0.0.0:8080

# Optional batching overrides. Keep these aligned with your throughput goals.
BATCH_SIZE=90
BATCH_LINGER_MS=20
MAX_INFLIGHT_BATCHES=200

# Optional logging override (defaults to `info`).
# RUST_LOG=info,ft_relay=info

# NOTE:
#   The FT contract account ID must be provided via the CLI flag:
#     ft-relay --token your-ft-contract.testnet
